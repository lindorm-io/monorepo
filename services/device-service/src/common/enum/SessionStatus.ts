export enum SessionStatus {
  ACKNOWLEDGED = "acknowledged",
  CODE = "code",
  CONFIRMED = "confirmed",
  EXPIRED = "expired",
  PENDING = "pending",
  REJECTED = "rejected",
  SKIP = "skip",
  VERIFIED = "verified",
}
