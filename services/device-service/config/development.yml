mongo:
  host: localhost
  port: 3301
  db_name: device
  username: root
  password: example

oauth:
  host: http://localhost:3500
  client_id: secret
  client_secret: secret

redis:
  host: localhost
  port: 3302

server:
  environment: development
  host: http://localhost
  port: 3300

services:
  communication_service: http://localhost:3200
