expiry:
  connect_identifier_session: 2 hours

mongo:
  host: localhost
  port: 27017
  db_name: identity
  username: username
  password: password

oauth:
  client_id: id
  client_secret: secret

redis:
  host: localhost
  port: 6379
  username: username

server:
  domain: https://lindorm.io
  environment: development
  issuer: https://identity.lindorm.io
  host: localhost
  port: 3000

services:
  communication_service:
    issuer: https://communication.lindorm.io
    host: localhost
  oauth_service:
    issuer: https://oauth.lindorm.io
    host: localhost
