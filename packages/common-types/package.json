{
  "name": "@lindorm-io/common-types",
  "description": "Common types for lindorm.io services",
  "version": "0.3.0",
  "license": "AGPL-3.0-or-later",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rimraf dist && tsc -b ./tsconfig.build.json",
    "test": "tsc --noEmit --watch",
    "test:ci": "tsc --noEmit",
    "test:update": "tsc --noEmit",
    "typecheck": "tsc --noEmit --watch",
    "typecheck:ci": "tsc --noEmit"
  }
}
