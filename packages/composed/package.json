{
  "name": "@lindorm/composed",
  "version": "0.1.5",
  "license": "AGPL-3.0-or-later",
  "author": "Jonn Nilsson",
  "repository": {
    "url": "https://github.com/lindorm-io/monorepo",
    "directory": "packages/composed"
  },
  "bugs": "https://github.com/lindorm-io/monorepo/issues",
  "publishConfig": {
    "access": "public"
  },
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "bin": {
    "composed": "dist/cli.js"
  },
  "scripts": {
    "build": "rimraf dist && tsc -b ./tsconfig.build.json && chmod +x dist/cli.js",
    "example": "ts-node example",
    "prettier": "prettier --write ./src/*",
    "test": "vitest run",
    "test:ci": "jest --forceExit",
    "test:smoke": "composed --file docker-compose.yml jest --config jest.config.smoke.mjs --runInBand --no-coverage",
    "test:update": "vitest run -u",
    "test:watch": "vitest",
    "typecheck": "tsc",
    "typecheck:watch": "tsc -w",
    "update": "ncu -i",
    "update:auto": "ncu -u",
    "verify": "npm run typecheck && npm run build && npm test"
  },
  "dependencies": {
    "commander": "^14.0.3"
  },
  "devDependencies": {
    "@types/pg": "^8.18.0",
    "pg": "^8.20.0"
  }
}
