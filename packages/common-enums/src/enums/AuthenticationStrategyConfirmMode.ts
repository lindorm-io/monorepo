export enum AuthenticationStrategyConfirmMode {
  DECIMAL = "decimal",
  EMAIL = "email",
  NONE = "none",
  NUMERIC = "numeric",
  SEARCH = "search",
  TEL = "tel",
  TEXT = "text",
  URL = "url",
}
