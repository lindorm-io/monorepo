/**
 * LINDORM EXTENSION. Coordinates behind the lindorm `geo_location` scope. No
 * RFC counterpart.
 */
export type GeoLocation = {
  /** wire: `lat` */
  lat: number;
  /** wire: `lng` */
  lng: number;
};
