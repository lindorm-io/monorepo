{
  "name": "@lindorm/iris",
  "version": "0.7.0",
  "license": "AGPL-3.0-or-later",
  "author": "Jonn Nilsson",
  "repository": {
    "url": "https://github.com/lindorm-io/monorepo",
    "directory": "packages/iris"
  },
  "bugs": "https://github.com/lindorm-io/monorepo/issues",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist"
  ],
  "bin": {
    "iris": "dist/cli.js"
  },
  "type": "module",
  "typings": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./scaffold": {
      "types": "./dist/scaffold.d.ts",
      "default": "./dist/scaffold.js"
    },
    "./mocks/jest": {
      "types": "./dist/mocks/jest.d.ts",
      "default": "./dist/mocks/jest.js"
    },
    "./mocks/vitest": {
      "types": "./dist/mocks/vitest.d.ts",
      "default": "./dist/mocks/vitest.js"
    }
  },
  "scripts": {
    "build": "rimraf dist && tsc -b ./tsconfig.build.json && chmod +x dist/cli.js",
    "example": "tsx example",
    "prettier": "prettier --write ./src/*",
    "test": "composed --file docker-compose.yml vitest run",
    "test:unit": "vitest run --config vitest.unit.mjs",
    "test:integration": "composed --file docker-compose.yml --project iris-integration vitest run --config vitest.integration.mjs",
    "test:tck:nats": "composed --file docker-compose.nats.yml --project iris-tck-nats vitest run --config vitest.tck.mjs nats",
    "test:tck:redis": "composed --file docker-compose.redis.yml --project iris-tck-redis vitest run --config vitest.tck.mjs redis",
    "test:tck:rabbit": "composed --file docker-compose.rabbit.yml --project iris-tck-rabbit vitest run --config vitest.tck.mjs rabbit",
    "test:tck:kafka": "composed --file docker-compose.kafka.yml --project iris-tck-kafka vitest run --config vitest.tck.mjs kafka",
    "test:tck:memory": "vitest run --config vitest.tck.mjs memory",
    "test:update": "composed --file docker-compose.yml vitest run -u",
    "test:watch": "composed --file docker-compose.yml vitest",
    "typecheck": "tsc",
    "typecheck:watch": "tsc -w",
    "update": "ncu -i",
    "update:auto": "ncu -u",
    "verify": "npm run typecheck && npm run build && npm test"
  },
  "dependencies": {
    "@inquirer/prompts": "^8.3.0",
    "@lindorm/aes": "^0.9.0",
    "@lindorm/errors": "^0.6.0",
    "@lindorm/is": "^0.3.0",
    "@lindorm/json-kit": "^0.8.0",
    "@lindorm/random": "^0.5.0",
    "@lindorm/retry": "^0.4.0",
    "@lindorm/scaffold": "^0.1.0",
    "@lindorm/scanner": "^0.6.0",
    "@lindorm/types": "^0.10.0",
    "@lindorm/utils": "^0.10.0",
    "commander": "^14.0.3"
  },
  "peerDependencies": {
    "@lindorm/amphora": ">=0.4.0",
    "@lindorm/kryptos": ">=0.11.0",
    "@lindorm/logger": ">=0.5.0",
    "amqplib": ">=0.10.0",
    "ioredis": ">=5.10.0",
    "kafkajs": ">=2.2.0",
    "nats": ">=2.29.0",
    "zod": ">=4.3.6"
  },
  "peerDependenciesMeta": {
    "@lindorm/amphora": {
      "optional": true
    },
    "@lindorm/kryptos": {
      "optional": true
    },
    "amqplib": {
      "optional": true
    },
    "ioredis": {
      "optional": true
    },
    "kafkajs": {
      "optional": true
    },
    "nats": {
      "optional": true
    }
  },
  "devDependencies": {
    "@lindorm/amphora": "^0.7.0",
    "@lindorm/kryptos": "^0.11.0",
    "@lindorm/logger": "^0.7.0",
    "@types/amqplib": "^0.10.8",
    "amqplib": "^0.10.9",
    "ioredis": "^5.10.0",
    "kafkajs": "^2.2.4",
    "nats": "^2.29.3",
    "zod": "^4.4.3"
  }
}
