{
  "name": "@lindorm-io/core-connection",
  "description": "Core Connection files for lindorm.io",
  "version": "0.1.15",
  "license": "AGPL-3.0-or-later",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -b ./tsconfig.build.json",
    "clean": "rimraf dist",
    "test": "jest",
    "test:update": "jest -u --",
    "test:watch": "jest --watch --",
    "typecheck": "tsc --noEmit",
    "typecheck:watch": "tsc --noEmit --watch"
  },
  "dependencies": {
    "@lindorm-io/core": "^0.8.8",
    "@lindorm-io/errors": "^0.1.16"
  },
  "devDependencies": {
    "@lindorm-io/core-logger": "^0.2.3"
  }
}
