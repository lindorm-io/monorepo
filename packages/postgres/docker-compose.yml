services:
  postgres:
    image: postgres:17
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: example
      POSTGRES_DB: default
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U root"]
      interval: 1s
      timeout: 3s
      retries: 10
