{
  "name": "@lindorm-io/middleware",
  "description": "Middleware composer for lindorm.io",
  "version": "0.0.1",
  "license": "AGPL-3.0-or-later",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "rimraf dist",
    "test": "jest",
    "test:update": "jest -u --",
    "test:watch": "jest --watch --",
    "typecheck": "tsc --noEmit",
    "typecheck:watch": "tsc --noEmit --watch"
  },
  "dependencies": {
    "clone": "^2.1.2"
  },
  "devDependencies": {
    "@types/clone": "^2.1.1"
  }
}
